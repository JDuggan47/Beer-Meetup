<div class="row">
  <div class="text-center">
    <% @beer.each do |item| %>
    <% if item.user_id == current_user.id %>
      <p>
        Name of Beer: <%= item.name_of_beer %>
      </p>
    <% end %>
  <% end %>
  <%= link_to "Back To Your Page", root_path %>
  </div>
</div>
